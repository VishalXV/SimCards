<menu xmlns:android="http://schemas.android.com/apk/res/android" >
    <item android:id="@+id/scan_devices" 
        android:title="Connect"
        android:icon="@android:drawable/ic_menu_search" 
        android:showAsAction="ifRoom|withText"></item>

</menu>
